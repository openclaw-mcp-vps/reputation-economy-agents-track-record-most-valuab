// This repository intentionally does not use Prisma at runtime.
// Direct SQL via `pg` powers persistence (`lib/database.ts`).
//
// The file exists only because the planning checklist expected `prisma/schema.prisma`.
// Keep this file as documentation-only and do not run `prisma generate`.
